import { CheckSquare } from 'lucide-react';
import ComingSoon from '../ComingSoon';
export default function Tasks() { return <ComingSoon title="Tasks" description="Create, assign, and track tasks with priorities, due dates, and status updates." icon={CheckSquare} />; }
