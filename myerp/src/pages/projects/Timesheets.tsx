import { Clock } from 'lucide-react';
import ComingSoon from '../ComingSoon';
export default function Timesheets() { return <ComingSoon title="Timesheets" description="Log time against projects and tasks, approve timesheets, and generate billing reports." icon={Clock} />; }
