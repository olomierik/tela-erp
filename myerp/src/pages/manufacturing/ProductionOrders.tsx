import { Workflow } from 'lucide-react';
import ComingSoon from '../ComingSoon';
export default function ProductionOrders() { return <ComingSoon title="Production Orders" description="Schedule production runs, track progress, and record completions with inventory updates." icon={Workflow} />; }
